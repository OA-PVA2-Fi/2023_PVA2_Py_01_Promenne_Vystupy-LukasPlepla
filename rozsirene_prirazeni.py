# Rozšířené přiřazení je jediný příkaz kombinující binární operaci a příkaz přiřazení, například +=, -= atd.

# Výraz rozšířeného přiřazení, jako je x += 1, lze přepsat jako x = x + 1, čímž se dosáhne podobného efektu.

number = 9.0
print("number = " + str(number))

number -= 2
print("number = " + str(number))


# Pomocí rozšířeného přiřazení přičtěte k číslu 5 a aktualizujte proměnnou.
number ??? 5

print("number = " + str(number))